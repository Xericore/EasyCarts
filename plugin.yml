name: EasyCarts
main: com.github.xericore.easycarts.EasyCarts
version: 1.2

commands:
   easycarts:
      description: Sets new minecart speed limit.
      usage: |
         /easycarts reload OR push|boost|maxspeed <multiplier>
permissions:
    easycarts.admin:
      description: Gives access to all easycarts features. 
      default: op